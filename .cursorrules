"Whenever you generate UI components, ensure they meet WCAG 2.1 AA standards. Use semantic HTML (e.g., <button> instead of <div>), ensure 4.5:1 contrast for text, and always include aria-label for icon-only components."
